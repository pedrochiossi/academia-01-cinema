window.onload = () => {
  
  const slider = tns({
    container: '.my-slider',
    items: 6,
    autoplay: false,
    loop: false,
    nav: false,
    slideBy: 'page',
    mouseDrag: true,
    controlsContainer: '#customize-controls'
  });

}
